# paths

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build paths` to build the library.

## Running unit tests

Run `nx test paths` to execute the unit tests via [Jest](https://jestjs.io).
